version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  jobs:
    pre_install:
      - pip install -U pip wheel setuptools

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
